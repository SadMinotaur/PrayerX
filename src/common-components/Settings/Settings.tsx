import React from 'react';
import SettingsIcon from './../../../assets/icons/settings.svg';
import {styles} from './styles';

export const Settings: React.FC = () => {
  return <SettingsIcon style={styles.icon} />;
};
