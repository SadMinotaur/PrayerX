import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  background: {
    flex: 1,
    backgroundColor: '#fff',
    alignItems: 'center',
  },
  textInput: {
    borderWidth: 1,
    width: 300,
    borderRadius: 6,
  },
  inputBorder: {
    margin: 20,
  },
});
